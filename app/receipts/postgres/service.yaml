apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: receipts
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None   # headless — StatefulSet обращается по DNS postgres.receipts.svc.cluster.local